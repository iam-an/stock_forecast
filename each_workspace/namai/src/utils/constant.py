from pathlib import Path

ROOT = Path(__file__).parent
OUTPUT = ROOT / "../../output"
CONFIG = ROOT / "./config.yaml"
RESULT = ROOT / "./result.json"
MODELS = ROOT / "../../../../models/"
COMPANIES = ROOT / "../../../../back/"
